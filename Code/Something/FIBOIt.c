#include<bits/stdc++.h>
#define DEBUG false
using namespace std;

const int N = 1e5+10;
int f[N];

main(){
    ///iterative -> for
    ///recursive -> recursive func
    ///fibo(n) = fibo(n-1)+fibo(n-2); fibo(0)=fibo(1)=1
    cin >> n;
    for(int i=1; i<=n; i++){

    }
}
